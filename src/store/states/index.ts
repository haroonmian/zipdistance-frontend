import { InitialStateType } from "../../types"

const initialState: InitialStateType = {
    zipcodes: [{
        id: null,
        zip: ''
    }],
    zipcodeDistances: []
}

export default initialState